/**
 * The product name. A name is not translated, so it is a constant rather than a
 * catalog entry — and having it here keeps it out of the interface as a bare
 * string, where the lint rule against literal text would have to guess.
 */
export const APP_NAME = 'Hybi';
