import { noLiteralUiText } from './no-literal-ui-text.js';

/** The repository's own rules, kept out of `node_modules` and out of npm. */
export default {
  rules: { 'no-literal-ui-text': noLiteralUiText },
};
